[project]
name = "pyinterpeter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pyinterp = "pyinterpeter.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pyright]
typeCheckingMode = "standard"
