# Task
- Reorganize all python files into categories under `python/<category>`
- If a python file is a library then put it in python `python/lib/<filename>.py` and rewrite imports
- Rewrite the README.md
## Rules
- Only .py files
- Do not modify files unless if imports in the file must be rewrited
- Must be all python files, mean the top-level directory must not contain any python files (100% no python files in top-level)

Thanks.
> Written with [StackEdit](https://stackedit.io/).
